version: "3.1"

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    #environment:
    #  MONGO_INITDB_ROOT_USERNAME: mongouser
    #  MONGO_INITDB_ROOT_PASSWORD: mongosecret
  redis:
    image: redis:alpine
    restart: always
    ports:
      - 6379:6379
